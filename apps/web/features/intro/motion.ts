/** Timing for the intro stage. Nothing on the intro reacts to the cursor. */
export const INTRO_MOTION = {
  /** Archer draw-and-release (morph + shot) before navigating. */
  leaveMs: 1700,
} as const;
